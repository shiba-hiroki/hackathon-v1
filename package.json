{
	"scripts": {
		"dev": "wrangler dev src/index.ts",
		"deploy": "wrangler deploy --minify src/index.ts",
		"check": "bunx @biomejs/biome check --apply src",
		"migration": "bun wrangler d1 migrations apply hackathon --local",
		"init": "bun wrangler d1 execute hackathon --local --file=\"./database/init.sql\"",
		"destroy": "rm -rf ./.wrangler"
	},
	"dependencies": {
		"@hono/swagger-ui": "^0.2.1",
		"@hono/zod-openapi": "^0.9.6",
		"drizzle-orm": "^0.29.4",
		"hono": "^4.0.3",
		"http-status-codes": "^2.3.0",
		"zod": "^3.22.4"
	},
	"devDependencies": {
		"@biomejs/biome": "1.5.3",
		"@cloudflare/workers-types": "^4.20240208.0",
		"drizzle-kit": "^0.20.14",
		"wrangler": "^3.25.0"
	}
}
